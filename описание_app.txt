dotenv = библиотека для работы с переменным окружения
  ( что б не указывать секретную инфу в явном виде)
mongoose = библиотека для работы с базами данных mongodb

cors = библиотека кросс запроса ( клиент и сервер на одном ip)

app.use функции обрабатывающие все запросы и передающие управление дальнейшей

